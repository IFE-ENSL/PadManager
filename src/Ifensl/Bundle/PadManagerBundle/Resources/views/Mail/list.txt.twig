Voici la liste de vos Cahiers:

- Cahiers privés:
{% for pad in padUser.ownPads %}
    Programme: {{ pad.program|raw }}, Unité d'enseignement: {{ pad.unit|raw }}, Titre: {{ pad.title|raw }}, url {{ url('ifensl_pad_show_private', {'slug': pad.slug, 'private_token': pad.privateToken}) }}
{% else %}
    Aucun pad
{% endfor %}

- Cahiers partagés:
{% for pad in padUser.pads %}
    Programme: {{ pad.program|raw }}, Unité d'enseignement: {{ pad.unit|raw }}, Titre: {{ pad.title|raw }}, url {{ url('ifensl_pad_show_public', {'slug': pad.slug, 'public_token': pad.publicToken}) }}
{% else %}
    Aucun pad
{% endfor %}