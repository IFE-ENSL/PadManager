{
    "items":[
        {% for unit in units %}
            {
            
                "id": {{ unit.id }},
                {% if unit.description %}
                    "description" : "{{ unit.description }}",
                {% endif %}
                "name": "{{ unit.name }}"
            {% if loop.last %}
                }
            {% else %}
                },
            {% endif %}
        {% endfor %}
    ]
}